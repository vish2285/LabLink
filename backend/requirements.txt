# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.0

# Data validation (Python 3.13 compatible)
pydantic==2.9.2

# Database
SQLAlchemy==2.0.36
python-dotenv==1.0.1      # For .env config

# HTTP client
httpx==0.27.2

# Matching (TF-IDF, cosine similarity) — optional for dev where build fails
# scikit-learn==1.5.0

# Optional utilities
numpy==1.26.4              # scikit-learn dependency (sometimes needs pinning)
rank-bm25==0.2.2           # BM25 lexical scoring

# Optional ML/DB (add when needed)
# pandas==2.2.3
# alembic==1.14.0
