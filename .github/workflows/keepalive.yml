name: keepalive

on:
  schedule:
    - cron: "*/15 * * * *"  # every 15 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health (HEAD)
        run: curl -fsSI https://lablink.onrender.com/health || true


